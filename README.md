# VulnLab

A deliberately vulnerable PHP web application for Red/Blue Team security training.

WARNING: This app is intentionally insecure. Do not deploy publicly.

See the `vulnerable` branch for the original vulnerable version, and the `main` branch for the patched version.